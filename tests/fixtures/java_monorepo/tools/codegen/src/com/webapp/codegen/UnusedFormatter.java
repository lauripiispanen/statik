package com.webapp.codegen;

/**
 * An unused class — should be detected as dead code.
 * No other file imports or references this class.
 */
public class UnusedFormatter {
    public String format(String input) {
        return input.trim().toLowerCase();
    }
}
